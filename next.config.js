/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'github.com', 'avatars.githubusercontent.com', 'images.ctfassets.net', 'yt3.googleusercontent.com'],
  }
}

module.exports = nextConfig
