import React from 'react';
import Link from "next/link";

const Logo = ({}) => {
  return (
    <Link href={'/'}>
      <h1 className="text-lg font-bold text-black">Chat With Anyone</h1>
    </Link>
  );
};

export default Logo;
// by Rokas with ❤️
